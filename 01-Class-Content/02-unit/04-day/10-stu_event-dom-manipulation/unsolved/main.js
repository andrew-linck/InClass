const foo = function(){
  console.log('called back');
}

const buttonList = document.querySelectorAll('.button');

for (let i = 0; i < buttonList.length; i++){

   // Add an event listener that listens for clicks on each element in the example list and runs the foo function
}