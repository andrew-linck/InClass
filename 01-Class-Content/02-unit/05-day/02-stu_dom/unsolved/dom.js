const $ = function () {
  //get the nodelist of every element with the class content
  const nodeList = document.querySelectorAll('.content');

  //text is now a function
  const text = function(content){
    //add your code here
  };
  const html = 'you';
  const on = 'guys';

  return {
    text: text,
    html: html,
    on: on
  };
}